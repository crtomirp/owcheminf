Packaged example workflows and teaching datasets.

This directory contains installed copies of selected `.ows` workflows and demo
CSV files so they remain available after `pip install`, not only in the source
repository checkout.
