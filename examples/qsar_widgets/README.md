# QSAR teaching example data

These CSV files are small educational datasets for the QSAR widget teaching worksheets. The target-like values are simplified educational values, not curated assay data.

Files:

- `qsar_training_set.csv` — labelled dataset with SMILES and educational target columns.
- `qsar_external_prediction_set.csv` — small external prediction set.
